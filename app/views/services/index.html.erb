<div class="main-container">

  <h1>Craftspeople</h1>
  <div class="service-content-center">

      <% @services.each do |service| %>
        <%# <%= link_to service_path(service) do %>
          <%= render "shared/service-card", service: service %>
        <%end%>
        <%# <%= link_to "Delete", service_path(service), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
      <%# <%end%>

  </div>
</div>

<div class="map">
  <%= render "shared/map"%>
</div>
